<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Portrait, the same decision the iPhone build makes: every screen in
             this app is a column, and a column turned on its side is a column with
             two hundred points of paper either side of it.

             `userPortrait` rather than `portrait` so somebody who has locked their
             phone upside down is obeyed. And Android 16 ignores an orientation
             request on a screen wider than 600dp anyway, which is the behaviour we
             want on a tablet and the reason this does not have to be conditional. -->
        <activity
            android:screenOrientation="userPortrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- What is deliberately not here: POST_NOTIFICATIONS, because push on Android
         goes through Firebase and there is no Firebase project yet, so asking for
         the permission would be asking for something the app cannot then do. It
         goes in with google-services.json, not before. -->
</manifest>

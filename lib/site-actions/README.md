Server actions the website's forms use — signing in, the newsletter, asking us
for a hand.

They lived beside the pages that call them, which was right until the website
moved under a language segment: their import paths would have become
`@/app/[lang]/(site)/account/actions`, which couples signing in to how the site
is routed, and they were never only the site's anyway — the members' app and the
back of the house both import the account ones.
